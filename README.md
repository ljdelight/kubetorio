This is a basic kubernetes controller, generated with Kubebuilder, for a Factorio server.
The code needs some tweaks to not break in most cases, and storage isn't persisted, so only use it for "fun".
